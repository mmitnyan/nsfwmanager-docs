# Scan Engine

The scan engine is the core component responsible for analysing files and producing NSFW classification scores.

## Responsibilities

<!-- What the engine does: image decoding, model inference, result normalisation. -->

## Model and Classification Categories

<!-- Description of the classification model and the output categories it produces. -->

## Supported File Types

<!-- Image and video formats the engine can process. -->

## Engine Pipeline

<!-- Step-by-step description of how a file moves through the engine: load → preprocess → infer → postprocess → emit result. -->

## Concurrency Model

<!-- How the engine handles multiple files in parallel without blocking the UI. -->

## Extensibility

<!-- How the engine could be updated or swapped in a future version. -->
